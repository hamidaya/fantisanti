import React from 'react';
import "./TermsConditions.css"

function TermsConditions ({styleState}) {

    return (

        <main className={styleState}>
            <section id="termsConditions" className="termsConditions">
                <p>TermsConditions page</p>
            </section>
        </main>
    );
}

export default TermsConditions;